# نظام الأدوار والقدرات

هذا الملف يوثّق نظام الأدوار الجديد (المرحلة الأولى من خطة التطوير). التعريف
الكامل والدقيق موجود دائمًا في `roles.py` (بيانات) و`abilities.py` (تنفيذ) —
هذا الملف ملخّص للقراءة السريعة فقط.

## البنية

- **`roles.py`**: 30 دورًا، كل دور كائن `Role` يحمل كل الحقول التي طُلبت:
  الاسم، الوصف، القصة الشخصية، الهدف العلني، الهدف السري، قالب السر
  الشخصي، القدرة الخاصة (اسم + وصف + عدد استخدامات + هل تحتاج هدفًا)،
  نقطة الضعف، معلومة أولية، علاقات محتملة مع أدوار أخرى، ومهمة جانبية.
- **`abilities.py`**: محرك تنفيذ موحّد. كل قدرة تمر عبر `use_ability()` الذي
  يتحقق من الصلاحية (دور نشط، استخدامات متبقية، هدف صالح إن لزم)، ثم ينفّذ
  المنطق الخاص بنوع القدرة (`ability_type`)، ثم يسجّل الاستخدام في قاعدة
  البيانات (`ability_log`) والذاكرة طويلة المدى، ويعيد نتيجة نصية واضحة.
- **`db.py`**: أعمدة جديدة على `players` (`role_id`, `ability_uses_left`,
  `last_ability_round`)، عمود `difficulty` على `games`، وجدولا
  `ability_log` و`protections`. تُضاف هذه عبر ترحيل (migration) آمن يفحص
  الأعمدة الموجودة فعليًا قبل الإضافة، فلا يكسر أي قاعدة بيانات قديمة.

## اختيار الأدوار وتوازنها

`roles.assign_roles(chat_id, player_count, difficulty)`:

- يتجنب الأدوار المستخدمة مؤخرًا في نفس المجموعة (بنفس أسلوب `variety.py`
  الموجود مسبقًا)، حتى تختلف المباريات المتتالية.
- يفرض **سقفًا** على عدد الأدوار "الفوضوية" (خداع/تضليل) بحساب
  `max(1, عدد اللاعبين // 5)`، حتى لا تطغى أدوار الخداع على اللعبة في
  المجموعات الصغيرة.
- يعيد بالضبط عدد الأدوار المطلوب، بلا تكرار لنفس الدور في نفس المباراة.

## القدرات المفعّلة في هذا الإصدار

كل الأدوار الثلاثين مفعّلة فعليًا (لا يوجد دور بقدرة "وهمية" أو placeholder).
أنواع القدرات المنفَّذة في `abilities.py`:

| نوع القدرة | ماذا تفعل فعليًا الآن |
|---|---|
| `investigate_evidence` | تكشف تفصيلًا إضافيًا حقيقيًا في دليل متداول |
| `analyze_evidence` | تقارن بين دليلين وتكشف اتفاقًا أو تناقضًا |
| `protect` | تسجّل حماية فعلية للاعب مستهدف لهذه الجولة في قاعدة البيانات |
| `spy_info` | تكشف آخر ما قاله لاعب مستهدف فعليًا في نقاش الجولة |
| `steal_info` | تكشف الهدف العلني الحقيقي للاعب مستهدف (دون سرّه الكامل) |
| `extra_question` | تولّد سؤال استجواب إضافيًا حقيقيًا يُضاف لسياق الجولة |
| `reveal_hint` / `puzzle_hint` | تولّد تلميحًا نصيًا من الذكاء الاصطناعي (أو fallback) |
| `unlock_secret_evidence` | تضيف دليلًا سريًا جديدًا فعليًا لقاعدة البيانات، معروفًا لصاحبه فقط |
| `propagate_rumor` | تضيف دليلًا مضلّلًا فعليًا (`reliability='misleading'`) |
| `recall_memory` | تستحضر حدثًا فعليًا من ذاكرة اللعبة الطويلة |
| `stats_report` | تحسب فعليًا الأكثر/الأقل مشاركة في نقاش الجولة الحالية |
| `neutralize_note` / `influence_decision` | تسجّل ملاحظة سردية تؤثر على سياق الجولة/القرار القادم |
| `passive` | لا قدرة تُستخدم يدويًا؛ تأثيرها سردي دائم (مذكور في بطاقة الدور) |

كل استخدام يُخصم من `ability_uses_left` ولا يمكن استخدام القدرة بعد نفاد
عددها؛ هذا يُختبر مباشرة في `tests/test_roles.py`.

## الأوامر الجديدة للاعبين

- **`/myrole`** *(في الخاص فقط)*: يعيد عرض بطاقة الدور الكاملة.
- **`/ability`** *(في الخاص فقط)*: يستخدم القدرة. إن احتاجت هدفًا، يعرض
  البوت لوحة أزرار باللاعبين لاختيار الهدف قبل التنفيذ.
- **`/case`**: سجل القضية (أحداث + أدلة + قرارات) — أي لاعب في المجموعة.
- **`/difficulty`**: للمشرف فقط، قبل اكتمال العدد وبدء الجولة، لضبط مستوى الصعوبة
  (`easy|medium|hard|expert|impossible`).

## حدود معروفة في هذا الإصدار (منظورة للمراحل القادمة)

- الحماية (`protect`) تُسجَّل فعليًا في قاعدة البيانات، لكن لا يوجد بعد
  نظام "أحداث ضارة تستهدف لاعبًا بعينه" لتتفاعل معه ميكانيكيًا؛ تأثيرها
  حاليًا سردي + مُسجَّل، وستُربط بنظام الأحداث المفاجئة في مرحلة لاحقة.
- الصعوبة (`difficulty`) تُخزَّن وتُعرض، لكنها لا تُغيّر بعد عدد الأدلة أو
  نسبة التضليل تلقائيًا؛ هذا مخطط له في مرحلة "المواقع والأحداث".
- أنظمة الألغاز الجماعية، المواقع، العلاقات الرسمية بين اللاعبين، والتصويت
  متعدد الأنواع، لم تُبنَ بعد كأنظمة مستقلة — هذه هي المراحل التالية
  المذكورة في `DEVELOPMENT_REPORT.md`.

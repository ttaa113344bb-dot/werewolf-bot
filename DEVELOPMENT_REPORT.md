# تقرير التطوير — المرحلة الأولى (نظام الأدوار والقدرات المتقدم)

> **ملاحظة تحديث لاحقة:** أمر `/begin` المذكور في هذا التقرير أُزيل في مرحلة
> تالية من التطوير (نظام الانضمام والبدء التلقائي). راجع `README.md` للسلوك
> الحالي: التسجيل يُغلق والجولة تبدأ تلقائيًا فور اكتمال عدد اللاعبين.

## 1) فحص المشروع الحالي (قبل التعديل)

المشروع كان بالفعل بنية جيدة ومنظمة نسبيًا:

- `app.py` — واجهة تيليجرام (aiogram v3): الأوامر والاستدعاءات (callbacks).
- `game.py` — محرك اللعبة: الجولات، النقاش، المراحل العميقة، التصويت،
  القرارات السرية، النهاية. جلسة مستقلة لكل مجموعة (`GameSession`)، بلا أي
  `sleep` يجمّد البوت كاملًا.
- `db.py` — طبقة SQLite (WAL + قفل asyncio + تنفيذ في thread منفصل).
- `ai_engine.py` — طبقة OpenAI مع fallback غني من `content_bank.py` عند
  غياب المفتاح أو فشل الاتصال.
- `memory.py` — ذاكرة قصيرة (نقاش الجولة) وطويلة (ملخصات مضغوطة) لتُرسل
  للذكاء الاصطناعي بدل تاريخ كامل غير محدود.
- `phases.py` / `variety.py` — منطق اختيار المراحل والتنويع ومنع التكرار.
- `tests/test_smoke.py` — اختبارات دخان جيدة تغطي: الانضمام، دورة لعبة
  كاملة، استقلالية عدة مجموعات، الإنهاء، الإيقاف المؤقت/الاستئناف/التمديد،
  الطرد، واستعادة الجولات المنقطعة بعد إعادة التشغيل.

**لم أجد أخطاء Syntax أو Import أو مشاكل قاعدة بيانات في الحالة الأصلية.**
الاختبارات السبعة الموجودة كانت تعمل جميعها بنجاح قبل أي تعديل.

### الفجوة الرئيسية بين الكود الموجود وطلبك

الدور (`role_name`) كان حتى الآن **نصًّا تجميليًا فقط** (اسم يُسند عشوائيًا
من قائمة 14 اسمًا في `content_bank.ROLE_NAME_POOL`)، بلا أي بيانات أو
ميكانيكا فعلية خلفه: لا هدف علني/سري، لا قدرة، لا نقطة ضعف، لا سر مرتبط
بالدور تحديدًا. هذا هو ما عالجته هذه المرحلة بالكامل.

## 2) خطة التطوير (وترتيب الأولوية)

طلبك يغطي 19 مرحلة ضخمة (أدوار، نقاش، استجواب، مواجهة، أدلة، ألغاز،
مواقع، أحداث، علاقات، تصويت، مهمات، قصص متفرعة، نهايات، تحليل، XP،
إحصائيات، سجل قضية، مشاهدون، إعدادات أدمن، عقل مدبر...). تنفيذ كل هذا في
جلسة عمل واحدة تنفيذًا حقيقيًا مُختبرًا (لا pseudo-code) غير واقعي دفعة
واحدة دون مخاطرة بجودة الكود أو استقراره — لذلك اتبعت توصيتك أنت بالذات:
**"لا تنفذ كل شيء بعشوائية؛ ابدأ بالأنظمة الأساسية ثم المتقدمة"**.

هذه المرحلة (الأولى من الخطة) ركّزت بالكامل على **الأنظمة الأساسية للأدوار
والأسرار والقدرات**، لأنها الأساس الذي تُبنى عليه كل بقية الأنظمة (الألغاز
الجماعية، المواقع، العلاقات الرسمية...). تم تنفيذها تنفيذًا حقيقيًا كاملاً،
لا اقتراحًا، مع اختبارات آلية تمر فعليًا.

## 3) الملفات التي تم تعديلها أو إضافتها

| الملف | التغيير |
|---|---|
| `roles.py` **(جديد)** | 30 دورًا كاملة البيانات + منطق اختيار وتوازن الأدوار |
| `abilities.py` **(جديد)** | محرك تنفيذ القدرات الأربعة عشر نوعًا |
| `db.py` | ترحيل آمن لأعمدة/جداول جديدة + دوال قراءة/كتابة الأدوار والقدرات والصعوبة |
| `game.py` | إسناد الأدوار عبر `roles.assign_roles`، إرسال بطاقة الدور، دمج سر الدور مع سر الذكاء الاصطناعي |
| `app.py` | أوامر جديدة: `/myrole`, `/ability` (+ لوحة اختيار هدف), `/case`, `/difficulty` |
| `README.md` | تحديث جدول الأوامر وإضافة إشارة للملفين الجديدين |
| `tests/test_roles.py` **(جديد)** | 7 اختبارات دخان لنظام الأدوار والقدرات |
| `tests/fakes.py` | توسعة الفلتر الوهمي `F` ليدعم `==`, `.startswith`, `&`, `~` فعليًا (كان يعطّل أي اختبار حقيقي لـ`app.py`، وهذا كشف أن `app.py` لم يكن مُختبرًا آليًا من قبل إطلاقًا) |
| `ROLES_AND_ABILITIES.md` **(جديد)** | توثيق نظام الأدوار والقدرات بالتفصيل |
| `DEVELOPMENT_REPORT.md` **(جديد)** | هذا التقرير |

**لم يُحذف أو يُعدَّل أي أمر أو ميزة موجودة سابقًا.** `variety.pick_role_names`
بقيت موجودة في `variety.py` (غير مستخدمة الآن من `game.py` لكن لم تُحذف،
حفاظًا على التوافق لأي كود آخر قد يستدعيها).

## 4) الميزات المضافة فعليًا (تنفيذ كامل، لا Placeholder)

- **30 دورًا** بكل الحقول المطلوبة: الاسم، الوصف، القصة الشخصية، الهدف
  العلني، الهدف السري، قالب السر الشخصي، القدرة (اسم + وصف + عدد
  استخدامات + هل تحتاج هدفًا)، نقطة الضعف، معلومة أولية، علاقات محتملة،
  مهمة جانبية.
- **اختيار أدوار متوازن**: يتجنب التكرار بين المباريات المتتالية لنفس
  المجموعة، ويحدّ من عدد الأدوار "الفوضوية" (خداع/تضليل) تلقائيًا حسب عدد
  اللاعبين.
- **بطاقة دور كاملة** تُرسل خاصًّا لكل لاعب عند بدء المباراة، ويمكن إعادة
  عرضها في أي وقت عبر `/myrole`.
- **سر شخصي مرتبط بالدور فعليًا** (لا نص عشوائي منفصل عن الدور): يُدمج سر
  الدور الثابت مع سر مولّد بالذكاء الاصطناعي (أو fallback) لضمان تنوع بين
  مباراة وأخرى حتى لحاملي نفس الدور.
- **محرك قدرات فعلي** لأربعة عشر نوع قدرة مختلف (تفصيلها في
  `ROLES_AND_ABILITIES.md`)، بعدد استخدامات محدود يُخصم فعليًا من قاعدة
  البيانات ويُمنع تجاوزه، مع دعم القدرات التي تحتاج اختيار هدف عبر لوحة
  أزرار Inline.
- **سجل القضية (`/case`)**: عرض الأحداث المهمة، الأدلة، والقرارات — أول
  خطوة فعلية من "المرحلة الخامسة عشرة" في طلبك.
- **إعداد الصعوبة (`/difficulty`)**: أول خطوة فعلية من "إعدادات الأدمن"
  و"الصعوبة" في طلبك (خمسة مستويات كما طلبت بالضبط)، مخزّنة لكل مجموعة.
- **حماية فعلية مسجَّلة في قاعدة البيانات** (`protections` table) تستخدمها
  أدوار الحارس/الطبيب/حارس الأسرار/الوريث، جاهزة ليُبنى عليها نظام
  الأحداث المفاجئة في المرحلة القادمة.

## 5) التحقق من عدم وجود أخطاء

تم تشغيل الفحوصات التالية فعليًا (لا افتراضًا):

```
python3 -m py_compile app.py db.py game.py roles.py abilities.py config.py \
    memory.py phases.py variety.py content_bank.py telegram_utils.py texts.py \
    ai_engine.py tests/*.py
→ نجح بلا أي خطأ Syntax

python3 -m tests.test_smoke     → 7/7 اختبارات ناجحة (لم تُكسر أي ميزة قديمة)
python3 -m tests.test_roles     → 7/7 اختبارات جديدة ناجحة
استيراد كل الوحدات معًا بما فيها app.py (تسجيل كل الأوامر والـ callbacks)
    → نجح بلا أي استثناء
```

قاعدة البيانات: الترحيل (migration) يفحص الأعمدة الموجودة فعليًا عبر
`PRAGMA table_info` قبل أي `ALTER TABLE`، ويُختبر أنه idempotent (تشغيله
عدة مرات لا يسبب خطأ) في `test_migrations_are_idempotent`.

## 6) طريقة التشغيل (لم تتغيّر)

```bash
pip install -r requirements.txt
cp .env.example .env   # وعدّل BOT_TOKEN على الأقل
python app.py
```

المتطلبات كما هي في `requirements.txt` (لم تُضَف أي مكتبة جديدة — كل ما
سبق مبني بالكامل على المكتبة القياسية + aiogram + openai الموجودَين
أصلًا):

```
aiogram>=3.4,<4.0
openai>=1.35,<2.0
python-dotenv>=1.0,<2.0
```

تشغيل الاختبارات:

```bash
python -m tests.test_smoke
python -m tests.test_roles
```

## 7) أوامر الأدمن (مع الجديد)

| الأمر | الوصف |
|---|---|
| `/begin` | بدء جولة جديدة |
| `/end` | إنهاء الجولة الحالية بأمان |
| `/pause` / `/resume` | إيقاف/استئناف كل مؤقتات الجولة |
| `/extend [ثواني]` | تمديد وقت النقاش |
| `/kick` (ردًّا على رسالة اللاعب) | إزالة لاعب قبل بدء الجولة |
| **`/difficulty [مستوى]`** *(جديد)* | ضبط مستوى الصعوبة قبل `/begin` |

## 8) خطة المراحل القادمة (خارطة طريق، غير منفَّذة بعد)

بالترتيب المنطقي الذي يبني كل مرحلة على سابقتها:

1. **نظام النقاش المتقدم + تحليل النقاش + الاستجواب + المواجهة** (مراحلك
   2–3 في الطلب): يبني على `phases.py`/`memory.py` الموجودَين، ويحتاج
   جدول جديد لتتبع الاتهامات/الدفاعات.
2. **نظام الأدلة المتقدم بالكامل** (خاص/عام/مشترك/سري + شروط ظهور
   وتحليل): توسعة لجدول `evidence` الحالي (مرتبط بما بدأناه في `abilities.py`).
3. **المواقع + الأحداث المفاجئة المرتبطة بالمنطق**: هنا تُربط `protections`
   المبنية في هذه المرحلة فعليًا بأثر ميكانيكي حقيقي.
4. **الألغاز الجماعية والقصة المتفرعة والنهايات المتعددة**: تحتاج محرك
   قرار جديد يتتبع "شجرة" اختيارات المجموعة.
5. **العلاقات الرسمية بين اللاعبين + أنواع التصويت المتعددة**.
6. **المهمات السرية + XP/المستويات + الإحصائيات والألقاب + وضع المشاهدين
   + بقية إعدادات الأدمن + نظام "العقل المدبر"**.

كل مرحلة قادمة ستُبنى فوق ما تم إنجازه هنا دون كسره، بنفس أسلوب الفحص
والاختبار الآلي المتّبع في هذه المرحلة.
